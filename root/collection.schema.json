{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://shadow.thruput.se/schemas/collection.schema.json",
  "title": "Collection",
  "description": "A collection extends a producer with a list of source slugs it is derived from",
  "allOf": [
    { "$ref": "producer.schema.json" }
  ],
  "properties": {
    "sources": {
      "type": "array",
      "items": {
        "$ref": "main.schema.json#/$defs/Slug"
      },
      "description": "List of source slugs this collection is derived from"
    }
  },
  "required": ["name", "slug", "concept"],
  "unevaluatedProperties": false
}
